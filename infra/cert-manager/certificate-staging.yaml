apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-mydomain-net-staging
  namespace: kube-system
spec:
  secretName: wildcard-mydomain-net-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - "example.com"
    - "*.example.com"
